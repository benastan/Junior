
// Define Init controller.
define(function() {

	// Logic we want to run only when the web site is first loaded.
	var init = {

		// Object with important methods, properties, etc.
	}
	return init;
});

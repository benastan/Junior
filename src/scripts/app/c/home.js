
//Define Home controller
define(function() {

	// Logic to run once, generating controller.
	var controller = function() {

		// Logic to run every time the controller is run.
	}
	return controller;
});
